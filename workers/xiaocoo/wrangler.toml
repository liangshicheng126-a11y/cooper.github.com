name = "xiaocoo-chat"
main = "src/index.ts"
compatibility_date = "2025-07-01"

# Optional custom domain: api.cooperliang.top → this worker
# [[routes]]
# pattern = "api.cooperliang.top/*"
# zone_name = "cooperliang.top"

[vars]
DEEPSEEK_MODEL = "deepseek-chat"
# ALLOWED_ORIGINS = "https://cooperliang.top,http://localhost:3000"

# Secrets (set via CLI, do not commit):
#   npx wrangler secret put DEEPSEEK_API_KEY
